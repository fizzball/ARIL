## Local test build (unsigned)

1. Quit ARIL completely
2. Open **ARIL-0.3.28.dmg** (or run `dist/ARIL.app`)
3. Toolbar chart → Model popularity should load without 404

## What's new in 0.3.28

- Solo gateway recycle also checks `/v1/models/rankings/weekly` so upgrades don’t keep a stale listener on :8741
