## Local test build (unsigned)

1. Quit ARIL completely (kills the Solo gateway on :8741)
2. Open **ARIL-0.3.24.dmg** (or run `dist/ARIL.app`)
3. New session → Auto → ask “what model is in use”
4. Expect an honest `openai/…` answer and footer like `[ gpt-4.1-mini · total (in+out) token cost = $… ]`

## What's new in 0.3.24

- Reply footer includes the real model leaf id
- Gateway injects an authoritative OpenRouter model-id note each turn (LLMs often mis-claim Claude/GPT)
- Note: Learning already showed those “Claude” turns were `openai/gpt-4.1-mini`
