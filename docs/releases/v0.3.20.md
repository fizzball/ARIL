## Local test build (unsigned)

1. Open **ARIL-0.3.20.dmg** (or run `dist/ARIL.app`)
2. Quit any running ARIL so the Solo gateway restarts
3. Preferences → MCP: enable DeepWiki (or other ready server), Check
4. Auto/Manual chat: ask something that needs the tool — look for `Using … · …` then the answer

## What's new in 0.3.20

- MCP tools in Auto/Manual chat (gateway tool loop + status in the reply)
