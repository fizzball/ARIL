## Local test build (unsigned)

1. Quit ARIL, open **ARIL-0.3.23.dmg** (or run `dist/ARIL.app`)
2. Manual → pick a model → send → switch to **Auto** → send again — Auto should use the profile/Prefer model, not the Manual lock
3. Preferences → Budget / Learning → Prefer wins + Run Auto eval as needed

## What's new in 0.3.23

- Budget guardrails (session/daily soft + hard USD caps)
- Other… Vision filter uses catalog modalities (name heuristic fallback)
- Learning: Category Prefer wins table + Run Auto eval (~8 prompts)
- Fix: Auto ignored leftover Manual `model` after mode switch
